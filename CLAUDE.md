# kaos-source Notes

- Keep discovery metadata-first.
- Do not inline large bodies.
- Materialization must flow through `kaos-core` artifacts.
- `kaos-source` should stay protocol-agnostic; MCP adaptation belongs in `kaos-mcp` or downstream tools.

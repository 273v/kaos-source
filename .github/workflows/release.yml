name: Release

on:
  push:
    tags:
      - "v*"
  # Manual fallback so a failed publish (e.g. a transient PyPI 5xx) can
  # be retried without re-tagging.
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build distribution
    runs-on: kaos-x64-16core
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install Python 3.13
        run: uv python install 3.13

      - name: Install dependencies
        run: uv sync --group dev --python 3.13

      - name: Verify version matches tag
        id: version
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          VERSION=$(uv run python -c "from kaos_source._version import __version__; print(__version__)")
          TAG=${GITHUB_REF_NAME#v}
          if [ "$VERSION" != "$TAG" ]; then
            echo "::error::Tag '$TAG' does not match _version.py value '$VERSION'"
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Pre-publish QA
        run: |
          uv run ruff format --check kaos_source tests
          uv run ruff check kaos_source tests
          uv run ty check kaos_source tests
          # Marker filter must mirror ci.yml exactly (F011). Integration
          # tests in tests/integration/ start subprocess MCP servers and
          # hit live APIs — neither runs cleanly in the release-job
          # environment.
          uv run pytest -m "not live and not network and not slow and not integration" --no-cov

      - name: Build wheel + sdist
        run: uv build

      - name: Verify metadata
        run: uvx --from twine twine check --strict dist/*

      - name: Smoke-test the built wheel in a clean venv
        run: |
          uv venv --python 3.13 /tmp/smoke
          uv pip install --python /tmp/smoke/bin/python dist/kaos_source-*-py3-none-any.whl
          /tmp/smoke/bin/python -c "
          import kaos_source
          # Public API surface that the wheel must expose.
          from kaos_source import (
              ArchiveConnector,
              FilesystemConnector,
              HttpConnector,
              MemoryConnector,
              SourceConnector,
              SourceLocator,
              SourceService,
              default_api_registry,
              default_connector_registry,
              default_parser_registry,
              register_source_tools,
          )
          # Default registries are populated on import — chain-imports
          # of connectors / apis / parsers must not error from the wheel.
          assert len(default_connector_registry) >= 5
          assert len(default_api_registry) >= 5
          assert len(default_parser_registry) >= 4
          # KSRC-05: EDGAR UA validation regression on the install path.
          from kaos_source.settings.edgar import KaosSourceEdgarSettings
          try:
              KaosSourceEdgarSettings(user_agent='no-at-sign').require_user_agent()
              raise AssertionError('KSRC-05 guard did not fire')
          except ValueError as exc:
              assert 'missing a contact email' in str(exc), str(exc)
          print(
              f'kaos-source {kaos_source.__version__}: '
              f'{len(default_connector_registry)} connectors, '
              f'{len(default_api_registry)} apis, '
              f'{len(default_parser_registry)} parsers, '
              f'KSRC-05 guard live'
          )
          "

      - name: Upload distribution artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 30

  publish-pypi:
    name: Publish to PyPI
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: kaos-x64-16core
    environment:
      name: pypi
      url: https://pypi.org/p/kaos-source
    permissions:
      id-token: write  # OIDC for Trusted Publishing
    steps:
      - name: Download distribution artifacts
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          attestations: true
          skip-existing: false

  github-release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: kaos-x64-16core
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download distribution artifacts
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          PRERELEASE_FLAG=""
          if [[ "$VERSION" == *a* || "$VERSION" == *b* || "$VERSION" == *rc* ]]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          gh release create "$GITHUB_REF_NAME" \
            --title "kaos-source $VERSION" \
            --notes-from-tag \
            $PRERELEASE_FLAG \
            dist/*
